# XLT Workbench 片段库与剪贴板历史 PRD

> 文档状态：待评审  
> 适用产品：XLT Workbench 桌面端  
> 范围：产品需求、信息架构、交互方案和技术边界

## 1. 摘要

片段库帮助用户长期保存并重复使用代码、命令、Prompt、链接和普通文本。剪贴板历史自动记录桌面端复制过的文本，并通过全局快捷面板完成搜索、复制和转存。

两项功能共用标签、搜索、置顶和复制反馈，但保持独立的数据生命周期：片段默认永久保存，剪贴板记录按保留策略自动清理。

## 2. 联系人

| 角色 | 负责人 | 责任 |
| --- | --- | --- |
| 产品负责人 | 待定 | 确认功能范围、默认值和验收结果。 |
| 交互与视觉 | 待定 | 确认主页、快捷面板和编辑器的交互一致性。 |
| Vue 开发 | 待定 | 实现列表、编辑、搜索、筛选和快捷键交互。 |
| Tauri / Rust 开发 | 待定 | 实现剪贴板监听、全局快捷键、本地存储和窗口调度。 |
| 测试 | 待定 | 验证数据安全、跨平台行为和键盘操作。 |

## 3. 背景

### 3.1 问题

开发者经常在 IDE、终端、浏览器和 AI 对话之间复制内容。常用命令、代码和 Prompt 分散在聊天记录、笔记和临时文件中，需要重新搜索或反复整理。用户也无法找回几分钟前被新内容覆盖的剪贴板文本。

XLT Workbench 已经是一个本地优先的开发者桌面工作台，并具备 Tauri 桌面壳、本地数据和托盘窗口基础。片段库和剪贴板历史与现有的 Git 报告、密钥库和 AI 工具管理具有相同的本地工作流。

### 3.2 产品原则

1. **本地优先**：内容只写入本机应用数据目录，不上传、不做远程同步。
2. **键盘优先**：搜索、选择、复制和转存都可以仅用键盘完成。
3. **主动收藏与临时历史分开**：片段库只收纳用户明确保存的内容，剪贴板历史承担自动记录。
4. **隐私状态可见**：采集是否开启、当前保留策略和排除规则必须对用户可见。
5. **不隐式执行内容**：命令和代码只能复制，应用不提供直接执行入口。

### 3.3 范围边界

V1 只支持文本剪贴板。图片、文件、富文本、跨设备同步、OCR、AI 自动分类和团队共享不进入 V1。

剪贴板监听和全局快捷面板仅在 Tauri 桌面端提供。浏览器开发模式不模拟系统剪贴板监听。

## 4. 目标

### 4.1 业务目标

- 让常用开发知识从“重新查找”变为“搜索后立即复制”。
- 让用户在剪贴板被覆盖后，可以通过一次快捷键找回近期文本。
- 形成“临时复制 → 反复使用 → 沉淀为片段”的本地闭环。

### 4.2 用户目标

- 用户可以在 3 秒内找到并复制一条已知片段。
- 用户可以在不离开当前应用的情况下召出剪贴板历史。
- 用户可以明确控制剪贴板采集、保留和删除。

### 4.3 关键结果

| 关键结果 | 目标 | 验证方式 |
| --- | --- | --- |
| 片段搜索到复制的中位操作时间 | 不超过 3 秒 | 本地事件耗时，只记录时长和结果，不记录内容。 |
| 快捷面板首次可交互时间 | 95% 的打开不超过 150 ms | 桌面端性能日志。 |
| 剪贴板连续采集成功率 | 不低于 99% | 复制脚本和人工跨应用测试。 |
| 用户明确排除的内容入库数 | 0 | 排除应用、暂停采集和隐私标记测试。 |
| 数据出站网络请求 | 0 | 开发和发布构建的网络行为检查。 |

## 5. 市场与用户场景

### 5.1 核心人群

核心用户是在多个工具间频繁切换的个人开发者。他们的共同问题是复制内容密度高、可复用知识分散，并且对源码、Token 和内部文本的隐私较敏感。

### 5.2 核心任务

| 任务 | 当前障碍 | 期望结果 |
| --- | --- | --- |
| 重复使用一条 shell 命令 | 从 shell history 或文档中翻找 | 搜索关键字后一键复制。 |
| 找回刚才复制的内容 | 系统剪贴板只保留当前值 | 召出快捷面板，按时间选择。 |
| 保存 AI 对话中的高价值内容 | 需要新建文件并手动分类 | 从剪贴板历史直接“保存为片段”。 |
| 限制敏感内容留存 | 剪贴板管理器可能记录密码或 Key | 支持暂停、排除应用和立即清空。 |

### 5.3 使用约束

- 用户可能长时间不打开主窗口，采集必须由桌面进程承担。
- 用户可能使用中英文、代码符号、URL 和多行文本搜索。
- 用户可能已占用建议的全局快捷键，因此快捷键必须允许修改并检测冲突。
- 密钥库复制出的密码和 Key 不进入剪贴板历史。

## 6. 价值主张

### 6.1 片段库

- 用一个统一入口管理代码、命令、Prompt、链接和短文本。
- 同时搜索标题、正文、语言和标签，不要求用户记住存储位置。
- 通过置顶、最近使用和使用次数，优先展示真正常用的内容。

### 6.2 剪贴板历史

- 使用全局快捷键召出，不打断当前工作流。
- 默认按时间分组，并对连续相同文本去重。
- 通过“保存为片段”把一次性内容变为长期知识。
- 用本地存储、排除规则和自动清理降低隐私风险。

### 6.3 差异化

XLT Workbench 不把这两项功能做成独立笔记软件。产品优先优化开发者的短内容复用，并与本机密钥库、Git 报告和 AI 工具管理共享一致的本地优先约束。

## 7. 解决方案

### 7.1 信息架构

主导航增加两个一级工作区，放在“Git 报告”和“密钥库”之间。这个位置将内容生产、内容复用和敏感信息管理串成连续的工作流。

```text
XLT Workbench
├─ 用量概览
├─ Git 报告
├─ 片段库
│  ├─ 全部片段
│  ├─ 置顶
│  ├─ 最近使用
│  ├─ 类型
│  └─ 标签
├─ 剪贴板
│  ├─ 历史
│  ├─ 置顶
│  └─ 采集设置
├─ 密钥库
├─ 用量分析
├─ 订阅与账单
└─ 连接与设置
```

主窗口提供完整管理能力。快捷面板只提供搜索、复制、置顶和转存，不承担批量管理。

### 7.2 片段库主页

#### 7.2.1 页面线框

```text
工作台 / 片段库                                  [+新建  ⇧⌘N]
代码片段

[🔍 搜索片段、命令或 Prompt                         ⌘ K] [全部] [最近] [📌置顶]
[筛选] [所有类型▾]                                             [最近使用▾]
标签  [全部标签] [git] [debug] [vue]

2 个片段                                                   点击片段复制到剪贴板
┌ [>_] Git 查看大文件                    [bash]      [置顶] [预览] [编辑] [删除] ┐
│ git cat-file -s HEAD:path/to/file                                                │
│ 命令 · git · 已复制 3 次                                                         │
└─────────────────────────────────────────────────────────────────────────────────┘
┌ [<>] Vue composable template       [typescript] [置顶] [预览] [编辑] [删除] ┐
│ const state = ref(...);                                                         │
│ 代码 · vue · 9/17 10:42                                                         │
└─────────────────────────────────────────────────────────────────────────────────┘
```

#### 7.2.2 布局和选择

- 片段库不显示主工作区的全局页头。页面以紧凑的工作栏和内容流为主，搜索框支持 `⌘K` 聚焦。
- 每个片段是独立的内容单元，显示类型图标、标题、语言和操作。代码与命令直接展示两至三行语法高亮预览；其他类型展示纯文本摘要。
- 点击内容单元立即复制。悬停或键盘聚焦时显示置顶、预览、编辑和删除操作，避免常驻按钮干扰阅读。
- “预览”在模态层中展示完整内容，并提供复制与编辑入口。代码和命令使用等宽字体及语法高亮，普通文本保留换行。

#### 7.2.3 新建与编辑

点击“新建片段”后，页面在筛选区下方展开可关闭的编辑区，不打断用户的浏览和搜索。编辑已有片段时使用模态对话框。从剪贴板历史转存时，编辑区自动填入正文，并要求用户确认标题和类型后保存。

| 字段 | 规则 |
| --- | --- |
| 标题 | 必填，1–120 个字符。从剪贴板转存时取首行的前 40 个字符作为可编辑初始值。 |
| 类型 | 必填，可选代码、命令、Prompt、文本、链接。 |
| 内容 | 必填，使用纯文本保存，保留换行和缩进。 |
| 语言 | 代码和命令类型显示，用于语法高亮和筛选。 |
| 描述 | 可选，记录用途、前置条件或注意事项。 |
| 标签 | 可选，逗号或回车创建，存储时去除重复和首尾空格。 |
| 置顶 | 可选，置顶片段在各自排序组内优先。 |

标题或内容为空时，“保存”按钮不可用。对话框可以通过取消或关闭按钮退出。

#### 7.2.4 搜索与排序

- 搜索范围包括标题、内容、描述、语言和标签。
- 多个搜索词按 AND 匹配，不区分英文大小写。
- 默认排序是“置顶优先，再按最近使用”。用户可切换为更新时间、创建时间、标题或使用次数。
- 没有结果时保留当前搜索和筛选条件，显示“清除筛选”和“新建片段”两个动作。

### 7.3 剪贴板历史主页

#### 7.3.1 页面线框

```text
┌─ 剪贴板 / Clipboard ────────────────────────────────────────┐
│ ● 正在记录  [搜索剪贴板历史…] [暂停] [清空历史] [设置] │
├──────────────────────────────┬──────────────────────────┤
│ 今天                             │ 内容预览               │
│ ★ pnpm typecheck               │                          │
│   Terminal · 10:42 · 使用 3 次   │ pnpm typecheck           │
│                                  │                          │
│   https://docs.rs/...             │ 来源  Terminal           │
│   Chrome · 10:38                  │ 时间  今天 10:42         │
│                                  │                          │
│ 昨天                             │ [复制] [保存为片段] [删除] │
│   const result = ...               │                          │
└──────────────────────────────┴──────────────────────────┘
```

#### 7.3.2 采集规则

1. V1 只记录不为空的纯文本，单条上限为 200 KB。
2. 新文本与已有记录完全相同时，不新建记录。系统更新最后复制时间和使用次数，并将该记录移到当前时间组顶部。
3. 应用暂停采集时，不读取也不入库新的剪贴板内容。状态栏和托盘菜单同时显示“已暂停”。
4. 密钥库写入剪贴板的内容带有应用内部排除标记，监听器必须跳过该内容。
5. 系统剪贴板包含 `transient` 或 `concealed` 语义标记时，监听器不得将内容写入数据库。
6. 来源应用位于用户排除列表时，监听器不得将内容写入数据库。
7. 默认保留 30 天或 1,000 条记录，任一上限达到后优先删除最旧的未置顶记录。置顶记录不参与自动清理。
8. 用户从片段库复制普通片段时，系统新建或更新一条剪贴板历史，并写入 `linkedSnippetId`。监听器对这次应用内部写入不再重复入库。

采集不使用正则表达式猜测密码或 Token。该方式会造成大量误判，不能代替剪贴板隐私标记、密钥库内部排除和用户的应用排除列表。

#### 7.3.3 列表与批量操作

- 记录按“今天、昨天、过去 7 天、更早”分组，组内按最后复制时间降序。
- 列表显示两行内容摘要、来源应用、时间和重复次数。
- 单击选中，双击复制。复制后记录移到列表顶部并增加使用次数。
- 用户进入多选模式后可以删除多条记录。删除操作先将记录隐藏 5 秒并显示“撤销”；撤销窗口结束后，事务才从数据库删除对应行。
- “清空历史”需要二次确认。确认框显示将删除的未置顶数量，用户可额外勾选“同时删除置顶内容”。

### 7.4 全局快捷面板

#### 7.4.1 打开与关闭

- 建议全局快捷键是 macOS 的 `Option + V` 和 Windows / Linux 的 `Alt + V`。用户在首次启用向导中确认后注册，并可以在设置中修改。
- 快捷键冲突时，设置页明确显示“已被其他应用占用”，快捷面板保持未注册状态。
- 面板在当前屏幕中心显示，打开后搜索框立即获得焦点。
- 按 `Esc`、再次按全局快捷键或点击面板外部时关闭面板，并将焦点还给先前的应用。

#### 7.4.2 面板线框

```text
┌──────────────────────────────────────────────────────┐
│ 🔍 搜索剪贴板或片段…                               │
├──────────────────────────────────────────────────────┤
│ 剪贴板                                             │
│ ▸ pnpm typecheck                  Terminal · 刚刚   │
│   const selected = computed(...)      VS Code · 3 分钟 │
│                                                    │
│ 片段                                               │
│   ★ Git 查看大文件               命令 · git      │
├──────────────────────────────────────────────────────┤
│ ↑↓ 选择   Enter 复制   ⌘S 保存为片段   Esc 关闭   │
└──────────────────────────────────────────────────────┘
```

空搜索时，面板显示最近 8 条剪贴板记录和最近 5 条片段。输入搜索词后，两类结果同时缩小并保留分组标题。

`Enter` 将选中内容写入系统剪贴板，关闭面板并返回前一个应用。V1 不模拟键盘粘贴，用户在目标应用中按系统粘贴快捷键完成插入。

#### 7.4.3 快捷键

| 位置 | 快捷键 | 行为 |
| --- | --- | --- |
| 全局 | `Option/Alt + V` | 打开或关闭快捷面板。 |
| 主窗口 | `Cmd/Ctrl + K` | 打开快捷面板并搜索片段与剪贴板。 |
| 片段库 | `Cmd/Ctrl + Shift + N` | 新建片段。 |
| 片段编辑器 | `Cmd/Ctrl + Enter` | 保存。 |
| 快捷面板 | `↑` / `↓` | 移动选中项。 |
| 快捷面板 | `Enter` | 复制选中内容并关闭。 |
| 快捷面板 | `Cmd/Ctrl + S` | 当选中结果来自剪贴板时，将其保存为片段。 |
| 快捷面板 | `Cmd/Ctrl + P` | 置顶或取消置顶。 |
| 快捷面板 | `Delete` | 当选中结果来自剪贴板时，删除该记录。 |
| 快捷面板 | `Esc` | 关闭并返回先前的应用。 |

### 7.5 关键用户流程

#### 7.5.1 找回并复制历史内容

```text
在任意应用中按全局快捷键
  → 快捷面板打开，搜索框聚焦
  → 输入关键词或使用 ↑↓ 选择
  → 按 Enter
  → 内容写入系统剪贴板，面板关闭
  → 在原应用中执行粘贴
```

#### 7.5.2 将剪贴板记录保存为片段

```text
选中一条剪贴板记录
  → 点击“保存为片段”或按 Cmd/Ctrl + S
  → 打开片段编辑器，自动填入标题和内容
  → 选择类型，补充语言、描述和标签
  → 保存
  → 保留原剪贴板记录，新片段出现在片段库顶部
```

#### 7.5.3 暂停采集

```text
在剪贴板主页或托盘菜单点击“暂停记录”
  → 状态更新为“已暂停”
  → 后续复制内容不读取、不入库
  → 用户点击“继续记录”后恢复
```

暂停状态在应用重启后保持，避免用户明确的隐私选择被重置。

### 7.6 空状态、反馈和错误

| 场景 | 界面行为 |
| --- | --- |
| 片段库首次打开 | 说明可保存的内容类型，提供“新建第一个片段”。 |
| 剪贴板历史为空 | 显示“尚未记录文本”和当前采集状态。 |
| 采集已暂停 | 顶部常驻黄色状态条，提供“继续记录”。 |
| 全局快捷键注册失败 | 设置页显示冲突键位和“重新录入”，不自动换用其他快捷键。 |
| 文本超过 200 KB | 不入库，在剪贴板页的采集状态中记录一次“已跳过超大文本”。 |
| 数据库写入失败 | 状态栏显示持续错误信息，停止新的采集写入，直到用户解决问题并重试。 |
| 搜索无结果 | 显示原搜索词和“清除搜索”，不展示无关内容。 |

### 7.7 设置

“连接与设置”增加“剪贴板”分组。剪贴板主页的“设置”按钮直接定位到该分组。

| 设置项 | 默认值 | 选项 |
| --- | --- | --- |
| 记录剪贴板 | 首次确认前关闭 | 开启 / 暂停 |
| 全局快捷键 | 建议 `Option/Alt + V` | 用户录入的组合键 |
| 保留天数 | 30 天 | 1、7、30、90 天或永久 |
| 最大条数 | 1,000 | 100、500、1,000、5,000 |
| 排除应用 | 密码管理器列表为空 | 从已检测的应用中多选 |
| 开机启动 | 关闭 | 开启 / 关闭 |

修改保留天数或最大条数时，页面在保存前显示预计删除的记录数。用户确认后立即执行新策略。

### 7.8 数据模型

内容保存到 Tauri 应用数据目录下的 SQLite 数据库。片段和剪贴板历史不写入 Web Storage。

```ts
type SnippetKind = "code" | "command" | "prompt" | "text" | "link"

interface Snippet {
  id: string
  title: string
  kind: SnippetKind
  content: string
  language: string | null
  description: string
  pinned: boolean
  useCount: number
  lastUsedAt: string | null
  createdAt: string
  updatedAt: string
}

interface Tag {
  id: string
  name: string
  normalizedName: string
}

interface ClipboardEntry {
  id: string
  content: string
  contentHash: string
  sourceAppId: string | null
  sourceAppName: string | null
  pinned: boolean
  copyCount: number
  firstCopiedAt: string
  lastCopiedAt: string
  linkedSnippetId: string | null
}

interface ClipboardPreferences {
  captureEnabled: boolean
  globalShortcut: string
  retentionDays: 1 | 7 | 30 | 90 | null
  maxEntries: 100 | 500 | 1000 | 5000
  excludedAppIds: string[]
  launchAtLogin: boolean
}
```

SQLite 使用 `snippets`、`tags`、`snippet_tags`、`clipboard_entries` 和 `clipboard_preferences` 五类表。搜索使用 SQLite FTS5，片段索引包括标题、内容、描述、语言和标签；剪贴板索引包括内容和来源应用名。

`contentHash` 用于精确去重，不作为安全或加密手段。数据库不保存密钥库复制出的敏感内容。

### 7.9 技术方案

#### 7.9.1 前端结构

```text
src/
├─ views/
│  ├─ SnippetLibraryView.vue
│  └─ ClipboardHistoryView.vue
├─ components/snippets/
│  ├─ SnippetList.vue
│  ├─ SnippetDetail.vue
│  └─ SnippetEditor.vue
├─ components/clipboard/
│  ├─ ClipboardList.vue
│  ├─ ClipboardDetail.vue
│  └─ ClipboardSettings.vue
├─ quick-panel/
│  ├─ main.ts
│  └─ QuickPanel.vue
├─ services/
│  ├─ snippet-service.ts
│  └─ clipboard-service.ts
└─ types/
   ├─ snippet.ts
   └─ clipboard.ts
```

Vue 页面只通过 service 调用 Tauri command，不直接组装 SQL。主窗口和快捷面板共用类型、搜索结果和设置模型。

#### 7.9.2 桌面端结构

```text
src-tauri/src/
├─ database.rs          # 数据库连接、迁移与事务
├─ snippets.rs          # 片段 CRUD、搜索、标签和使用记录
├─ clipboard.rs         # 采集、去重、保留策略和写回剪贴板
└─ quick_panel.rs       # 全局快捷键和快捷窗口调度
```

- Rust 进程负责剪贴板变化检测，不依赖主窗口是否可见。
- 采集、去重和数据库写入位于同一个事务边界内，避免重复记录。
- 全局快捷键由 Tauri 全局快捷键能力注册。快捷面板使用独立窗口，不复用当前托盘用量面板。
- 来源应用识别使用操作系统前台进程信息。用户未授予相关系统权限时，来源字段保持为空，页面明确显示“未记录来源应用”。
- 数据库创建、版本迁移和 FTS 索引更新必须有自动化测试。

### 7.10 安全与隐私

- 首次打开剪贴板功能时，应用在启用前解释采集内容、保留时间和排除方式。
- 剪贴板数据不参与任何远程请求、AI 处理或产品分析上报。
- 应用不记录用户的搜索词、片段内容或剪贴板内容到普通日志。
- 片段库是普通本地知识库，不提供应用层加密。编辑器在片段类型附近提示用户将密码、Token 和 API Key 保存到密钥库。
- “密钥库 → 剪贴板”通路必须携带内部排除标记，并保留现有 30 秒后清空策略。
- 单条或批量删除的 5 秒撤销窗口结束后，数据库必须删除对应行。清空历史在用户二次确认后立即删除。应用不保留可还原的内容副本。

### 7.11 可访问性

- 所有列表、菜单、抽屉和对话框都必须支持键盘焦点移动。
- 选中、置顶、采集开启和错误不能只使用颜色表达。
- 快捷键提示使用当前操作系统的键位名称。
- 复制、删除、暂停和恢复采集的反馈通过 `aria-live` 向辅助技术播报。

### 7.12 验收标准

#### 片段库

- 用户可以创建、编辑、复制、置顶和删除五类片段。
- 搜索可匹配标题、内容、描述、语言和标签。
- 从剪贴板转存后，新片段内容与原文完全一致，换行和缩进不丢失。
- 置顶片段在默认排序中优先显示，复制后更新使用次数和最近使用时间。

#### 剪贴板历史

- 在桌面进程运行且采集开启时，从支持的桌面应用复制文本后能在历史中找到。
- 连续复制相同文本只保留一条记录，同时更新时间和计数。
- 暂停期间、排除应用和密钥库产生的内容不进入数据库。
- 保留策略能清理过期或超量的未置顶记录，不删除置顶记录。
- 全局快捷面板可以搜索两类内容，并用键盘完成选择和复制。

#### 数据与隐私

- 片段、剪贴板和搜索词不出现在应用日志或网络请求中。
- 应用重启后保留片段、剪贴板置顶、暂停状态和用户设置。
- 清空历史后重新查询数据库，不存在被清空的记录。

### 7.13 假设与待验证项

| 假设 | 风险 | 验证方式 |
| --- | --- | --- |
| 用户在理解采集范围后愿意主动开启剪贴板记录 | 隐私感受不符合预期 | 首次启用确认与用户访谈。 |
| 30 天 / 1,000 条是合理默认值 | 记录不够用或数据留存过多 | 观察本地条数分布和手动调整比例，不收集内容。 |
| `Option/Alt + V` 冲突率可接受 | 用户无法召出面板 | 在 macOS、Windows 和常见启动器环境下测试。 |
| 文本类型能覆盖首期主要场景 | 用户对图片或文件历史需求更高 | 记录用户手动反馈，不读取未支持的剪贴板内容。 |
| 复制后手动粘贴可以接受 | 比自动粘贴多一步 | 对快捷面板完成时间和反馈进行可用性测试。 |

## 8. 发布计划

### 8.1 迭代一：片段库基础

预计 1 个开发迭代。

- 建立 SQLite 数据库和迁移框架。
- 完成片段 CRUD、标签、搜索、置顶、排序和复制。
- 完成主导航、三栏页面和编辑抽屉。
- 完成服务层、Rust command 和数据库测试。

### 8.2 迭代二：剪贴板采集与管理

预计 1 个开发迭代。

- 完成文本采集、去重、搜索、分组、置顶和删除。
- 完成暂停、保留策略、排除应用和密钥库排除通路。
- 完成“保存为片段”。
- 在 macOS 和 Windows 上进行跨应用采集测试。

### 8.3 迭代三：全局快捷面板

预计 1 个开发迭代。

- 完成独立快捷窗口、全局快捷键和冲突提示。
- 完成片段与剪贴板的联合搜索。
- 完成纯键盘选择、复制、置顶和转存。
- 验证打开速度、焦点恢复和多显示器定位。

### 8.4 后续候选功能

以下能力在 V1 数据和用户反馈明确后再排期：

- 图片、文件和富文本剪贴板历史。
- 经用户授权的自动粘贴。
- 片段变量和填空模板。
- 片段导入、导出和加密备份。

- 经用户明确选择的 AI 摘要、标签和重复内容归并。
- 适用于剪贴板的托盘快捷列表。

### 8.5 发布前检查

- [ ] 产品负责人确认默认保留时间、条数上限和全局快捷键。
- [ ] 视觉评审覆盖主页、编辑抽屉、快捷面板、空状态和错误状态。
- [ ] 密钥库复制内容在所有平台上均不进入剪贴板历史。
- [ ] 排除应用、暂停采集和系统隐私标记具有自动化测试。
- [ ] 数据库初始化、迁移、搜索索引和保留清理具有自动化测试。
- [ ] 全局快捷键在 macOS 和 Windows 上通过冲突、锁屏和多显示器测试。
- [ ] `pnpm typecheck`、单元测试和桌面端集成测试全部通过。
